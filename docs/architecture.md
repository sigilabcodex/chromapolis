# Chromapolis MVP Notes

- Frontend-only, static-first architecture.
- No analytics, tracking scripts, account system, or cookies by default.
- Future map integration can be implemented as an optional UI layer.
- Future dataset ingestion should load local files from `data/` for local-first usage.
